version: "3.8"

volumes:
  dachasql:

services:
  db:
    build: ./db/.
    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "YwolAaP6dBLM"
      MSSQL_PID: Express
    ports:
      - "1433:1433"
    volumes:
      - dachasql:/var/opt/mssql
    restart: always
  api:
    build: .
    depends_on:
      - db
    ports:
      - "5000:80"
    restart: always